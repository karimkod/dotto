<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:label="Dotto"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <!-- Dotto's live AdMob application id for Android. Must match the
             account the Android unit ids in lib/ads/ad_manager.dart belong to,
             or those units never fill. The app crashes at startup if this
             meta-data is missing entirely. -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3605343790686215~6715217582"/>
        <!-- No Consent Mode defaults declared here. UMP owns the consent state
             and emits those signals itself; hardcoded defaults would be a
             second source of truth for the same thing, and one that cannot be
             updated when the player answers the form. -->
        <!-- Play Games Services. Without this the SDK fails as it starts, which
             on this app means at launch — see lib/services/game_services.dart.
             The value must stay a @string reference: an all-digits literal is
             parsed as an integer and overflows. -->
        <meta-data
            android:name="com.google.android.gms.games.APP_ID"
            android:value="@string/app_id"/>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Which channel a pushed (FCM) notification lands on when the message
             does not name one. Ours name "challenges"; this is the fallback so
             a message sent without one is not dropped on Android 8+. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="challenges" />
        <!-- The status bar icon for a pushed notification. Without this Android
             falls back to the launcher icon, and a launcher icon in the status
             bar is the grey square everyone recognises as an app that did not
             supply one: the system keeps only the alpha and the full-bleed
             background becomes a solid block.

             ic_notification is therefore a white silhouette on transparency,
             not artwork — see scripts/make_notification_icon.py. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_notification" />
        <!-- Tints that silhouette, and the app name beside it, in the shade.
             Only applies to messages that do not carry their own colour. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/notification_color" />
        <!-- Scheduled reminders are hours or days out, so a reboot inside that
             window would otherwise drop them silently: Android clears the alarm
             table on restart. These receivers re-register whatever was pending.
             flutter_local_notifications ships the classes but declares neither
             them nor RECEIVE_BOOT_COMPLETED — the app has to. -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="false" />
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- POST_NOTIFICATIONS and VIBRATE arrive by manifest merge from
         flutter_local_notifications. This one does not. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
